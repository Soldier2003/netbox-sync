# CHANGELOG_FORK.md

Форк `bb-Ricardo/netbox-sync` для инфраструктуры Lense (netbox.seclab.local + vCenter_DEMO).

Базовая версия: локально установленный **v1.8.0** (2025-03-07), точечно доведённый
до состояния **v1.8.1** (2026-03-18) там, где это подтверждено проверкой исходников,
плюс 6 собственных фиксов, найденных при разборе `netbox-sync-logs` (18–20.07.2026)
и при первых реальных прогонах форка на сервере (21.07-23.07.2026).

**История ревизий этого документа:**
- **23.07.2026, ред. 5:** добавлен собственный фикс 2.6 — баг в самом
  self-heal кабелей (п. 2.3): успешное удаление кабеля (NetBox отвечал 204)
  затем валилось с `AttributeError` при попытке залогировать имя объекта,
  и это гасило повторный PATCH интерфейса. Баг присутствовал с момента
  написания фикса 2.3, но не был замечен раньше — по факту NetBox
  Changelog кабель действительно исчезал (DELETE прошёл), поэтому только
  разбор полного текста лога боевого прогона против NetBox 4.6.4
  (23.07.2026) обнаружил, что PATCH после этого не повторялся.
- **21.07.2026, ред. 4:** добавлен собственный фикс 2.5 — конфликт устаревшего
  `tagged_vlans` при смене режима интерфейса на `tagged-all` (найден по факту
  первого боевого, не dry-run прогона; подтверждено, что ошибка уже
  присутствовала в логах 20.07.2026, до всех правок форка — не регрессия).
- **21.07.2026, ред. 3:** добавлен собственный фикс 2.4 — необработанный 5xx
  NetBox при постраничном GET-запросе ронял весь прогон необработанным
  traceback'ом вместо понятной ошибки. Найден по факту первого реального
  dry-run прогона форка на сервере (не в логах прошлых прогонов v1.8.0 —
  там пагинация той же коллекции, видимо, укладывалась в одну страницу или
  не совпадала по времени с 502 от NetBox).
- **21.07.2026, ред. 2:** добавлен перенос фикса "wrong platform evaluation"
  (#448/#492/#495) — в предыдущей редакции он ошибочно попал в общий список
  "не перенесено" без отдельного обоснования применимости; уточнено, что
  `aiodns==4.0.0` ставится и работает на Python 3.10 (не только 3.12+), значит
  порядок шагов деплоя (сейчас/после апгрейда ОС) не конфликтует.

Все находки в этом документе основаны на:
- фактическом коде `opt/netbox-sync/module/` из проекта (version 1.8.0);
- фактических логах `netbox-sync-logs/*.log` (18–20.07.2026);
- сравнении с исходниками upstream на тегах `v1.8.0` и `v1.8.1`
  (github.com/bb-Ricardo/netbox-sync), полученными напрямую из репозитория;
- официальными issue/PR/release notes GitHub (ссылки указаны у каждого пункта).

---

## 1. Перенесено из официального upstream v1.8.1

netbox-sync **не заброшен полностью**: несмотря на issue о сворачивании поддержки
([#474](https://github.com/bb-Ricardo/netbox-sync/issues/474), открыт 23.07.2025,
срок архивации был обозначен 31.10.2025), 18.03.2026 вышел ещё один официальный
релиз — **v1.8.1**. Это на 5 месяцев позже "дедлайна архивации" — то есть автор
по факту продолжает изредка мержить фиксы. Из него перенесены 5 точечных
изменений (только то, что реально проверено построчным сравнением, не весь релиз
целиком — что осознанно не перенесено и почему, см. ниже):

| Файл | Что изменено | Зачем |
|---|---|---|
| `module/__init__.py` | версия `1.8.0` → `1.8.1-lense.1` | трассируемость версии в логах |
| `requirements.txt` | `aiodns` `3.0.0` → `4.0.0`, остальные зависимости подтянуты к пинам v1.8.1 | Исправляет `TypeError: Channel.gethostbyaddr() takes 2 positional arguments but 3 were given` — но эта ошибка проявляется только под Python 3.12+ (после перехода на Ubuntu 24.04). Отдельно проверено на PyPI: `aiodns==4.0.0` официально поддерживает и Python 3.10 (classifier "Requires: Python >=3.10"), и у `pycares==5.0.1` есть готовые wheel-пакеты под `cp310`/`manylinux_2_28` — то есть ставится и работает уже сейчас, на текущем сервере, без сюрпризов при апгрейде Python позже. Issue [#488](https://github.com/bb-Ricardo/netbox-sync/issues/488) |
| `netbox-sync.py` | добавлен блок "disable pruning if an enabled source is unavailable" | **важно для вас**: в логах зафиксировано 19 обрывов связи с vCenter ("No route to host"). Без этого фикса кратковременная недоступность vCenter могла привести к тому, что netbox-sync посчитает все объекты источника пропавшими и **удалит** их (prune). Фикс автоматически отключает prune на прогонах, где источник недоступен. Issue [#490](https://github.com/bb-Ricardo/netbox-sync/pull/490) |
| `module/netbox/connection.py` → `get_api_version()` | теперь версия NetBox сначала читается из JSON `/api/status/` (`netbox-version`), и только потом — из HTTP-заголовка `API-Version` | исправляет "Missing API-Version" — часть NetBox-инсталляций/прокси не отдают кастомный заголовок. Issue [#446](https://github.com/bb-Ricardo/netbox-sync/issues/446) |
| `module/sources/vmware/connection.py` → `add_virtual_machine()`, определение `platform` | переписана логика определения ОС виртуальной машины: теперь сначала читается `guestInfo.detailed.data` (живые данные от VMware Tools), и только потом, как fallback, — `guestOS.detailed.data` (может быть устаревшим/сконфигурированным значением); для Linux-гостей к platform добавляется версия дистрибутива; добавлена защита от "мусорных" значений, если guest tools вернули текст ошибки вместо ОС | **применимо к вашей среде**: у вас источник `vCenter_DEMO` синхронизирует и хосты, и ВМ (в т.ч. `kali`, `pt-dephaze-server` — видно в логах), platform для них собирается именно этим кодом. Старая версия читала только `guestOS.detailed.data`, из-за чего платформа ВМ могла показывать устаревшую версию ОС (пример из upstream issue: ВМ обновили с RHEL 7 до RHEL 8, а netbox-sync продолжал писать "Red Hat Enterprise Linux 7"). Issues [#448](https://github.com/bb-Ricardo/netbox-sync/issues/448), [#492](https://github.com/bb-Ricardo/netbox-sync/issues/492) |

**Осознанно не перенесено:**

- **check_redfish 2.0 (#478)** — не применимо: в `settings.yaml` источник
  `check_redfish` не сконфигурирован, используется только `vCenter_DEMO`
  (type: vmware). Если когда-нибудь добавите redfish-источник — тогда стоит
  вернуться к этому пункту.
- **Конфликт зависимостей с `vsphere-automation-sdk` (#481, #497)** — не
  применимо сейчас: этот SDK нужен только для синхронизации vCenter-тегов
  (`cluster_tag_source`/`host_tag_source`/`vm_tag_source`), а в `settings.yaml`
  все три опции закомментированы (строки 310-312 и 556-558) — то есть tag
  sync выключен, SDK не устанавливается и не используется. **Если решите
  включить синхронизацию тегов из vCenter** — сначала проверьте issue
  [#481](https://github.com/bb-Ricardo/netbox-sync/issues/481) и
  [#497](https://github.com/bb-Ricardo/netbox-sync/issues/497) на предмет
  актуального статуса конфликта, и напишите — разберём отдельно и перенесём
  нужный фикс в форк.

---

## 2. Собственные фиксы (найдены в ваших логах, отсутствуют в upstream v1.8.1)

Все три проверены построчно и против исходников v1.8.1 — баг присутствует
**и там**, то есть это не "уже исправлено выше по течению", а реальный gap в
самом проекте.

### 2.1. MAC-адрес не отвязывается от интерфейса (~76 ошибок за прогон)

**Файл:** `module/netbox/object_classes.py`, класс `NBMACAddress.remove_interface_association()`

**Симптом в логах** (пример, `netbox-sync-20260719-0030.log:166-181`):
```
ERROR: NetBox returned: PATCH /api/dcim/mac-addresses/1418/ Bad Request
ERROR: NetBox returned body: {'__all__': ['Cannot unassign MAC Address while it is designated as the primary MAC for an object']}
```

**Причина:** в NetBox 4.2+ поле `primary_mac_address` принадлежит **интерфейсу**
(`NBInterface`/`NBVMInterface`), а не устройству/ВМ — в отличие от `primary_ip4`/
`primary_ip6`, которые действительно являются полями устройства. Метод
`remove_interface_association()` скопирован по образцу IP-адресного аналога и
проверяет `primary_mac_address` на **устройстве** (`get_device_vm()`), у
которого такого поля просто нет ни в `NBDevice`, ни в `NBVM.data_model`. В
результате условие никогда не срабатывает, `primary_mac_address` на интерфейсе
не очищается, и последующая попытка отвязать MAC от интерфейса отклоняется
NetBox — потому что он **действительно всё ещё** назначен как primary.

Отдельно от этого в самом NetBox была смежная, но другая ошибка —
false-positive той же проверки даже для НЕ primary MAC — issue
[#18768](https://github.com/netbox-community/netbox/issues/18768), исправлена
в PR [#18784](https://github.com/netbox-community/netbox/pull/18784). Проверьте
после апгрейда, не остались ли ошибки — если да, это будет означать, что ваша
версия NetBox 4.6.x ещё не содержит этот PR.

**Фикс:** проверять и очищать `primary_mac_address` на объекте, возвращаемом
`get_interface()`, а не `get_device_vm()`.

### 2.2. Дублирующийся asset_tag "Base Board Asset Tag" (~38 ошибок за прогон)

**Файл:** `module/sources/vmware/connection.py`, сбор `AssetTag` для ESXi-хоста

**Симптом в логах:**
```
ERROR: NetBox returned body: {'asset_tag': ['device with this asset tag already exists.']}
```

**Причина:** несколько ESXi-хостов (в логах — как минимум esxi05, esxi06)
отдают через `otherIdentifyingInfo` **одинаковую** строку `"Base Board Asset
Tag"` — это стандартное значение по умолчанию, которое AMI/Supermicro
прошивка кладёт в SMBIOS Type 2 (Base Board Information), если в BIOS никто не
вписал реальный asset tag вручную. Список `banned_tags` в коде уже отфильтровывает
похожие плейсхолдеры ("Default string", "to be filled by o.e.m." и т.п.), но
конкретно эту строку — нет. NetBox требует глобальной уникальности `asset_tag`,
поэтому второй и последующие хосты с этим значением стабильно отклоняются.

**Фикс:** добавлены `"Base Board Asset Tag"`, `"Chassis Asset Tag"` (тот же
паттерн для SMBIOS Type 3), `"Asset Tag"`, `"Not Specified"`, `"Not Set"`,
`"Fill By OEM"` в `banned_tags`; сравнение сделано по `.strip()`, чтобы пустая
строка/пробелы тоже не считались валидным тегом.

**Что стоит сделать отдельно:** это фильтрует значение **на будущее**. Если в
NetBox уже записан `asset_tag = "Base Board Asset Tag"` на каком-то устройстве
из прошлых прогонов, стоит один раз вручную очистить это поле в UI/API у
затронутых устройств (в логах видно `esxi05.seclab.local`, `esxi06.seclab.local`,
device id 409 и 411) — иначе они просто перестанут обновляться, но старое
неверное значение останется.

### 2.3. "Virtual interfaces cannot have a cable attached" (~93 ошибки за прогон)

**Затронутые интерфейсы (device dcim/interfaces id, из логов):**
4483, 4490, 4491, 4492, 4494, 4496, 4497 (vmk0/vmk3/vmk4 на нескольких хостах).

**Причина:** netbox-sync вообще не читает и не пишет поле `cable` — это
подтверждено (`grep -i cable` по всему `module/` не находит ни одного
обращения ни в вашей установленной версии, ни в исходниках v1.8.1 с GitHub).
Значит ошибка возникает только потому, что в самом NetBox на этих интерфейсах
**уже стоит кабель** (создан вручную или остался от какой-то более старой
интеграции/ошибки), а netbox-sync корректно синхронизирует их как
`type: virtual` (VMkernel-порты — логические, не физические) — а NetBox
запрещает физическое подключение кабеля к неподключаемому (`non-connectable`)
типу интерфейса.

**Фикс (добавлен в `module/netbox/connection.py`):** сделан "самолечащийся"
retry — при получении именно этой ошибки на PATCH интерфейса netbox-sync:
1. запрашивает текущий объект интерфейса, чтобы узнать `cable.id`;
2. удаляет этот кабель через `DELETE /api/dcim/cables/{id}/`;
3. повторяет исходный PATCH один раз.

Если что-то в этой цепочке пойдёт не так — логируется предупреждение, и
поведение откатывается к прежнему (ошибка просто логируется, прогон
продолжается, как раньше). Кабель не пытается быть восстановлен — если он
был нужен, то это укажет на то, что интерфейс в vCenter в реальности не
"virtual", и тогда проблему нужно разбирать на уровне классификации
типа интерфейса, а не на уровне кабеля.

**Рекомендация:** это устраняет симптом автоматически, но стоит один раз
проверить в NetBox UI, откуда вообще взялись эти кабели на vmk-портах —
похоже на историческую ошибку ручного ввода или более раннюю версию скрипта.

### 2.4. Необработанный 5xx NetBox при постраничном запросе роняет весь прогон

**Файл:** `module/netbox/connection.py`, `NetBoxHandler.request()` и `single_request()`

**Симптом** (первый реальный dry-run форка на сервере, 21.07.2026):
```
DEBUG2: Received HTTP Status 502.
Traceback (most recent call last):
  ...
json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
```
при запросе очередной страницы объектов "MAC addresss" (1358 объектов,
несколько страниц пагинации).

**Причина:** метод `request()` проверяет `response.status_code == 200`
только для **первой** страницы. Для всех последующих страниц пагинации
(`while response.json().get("next")...`) код безусловно вызывает
`response.json()` — если NetBox или прокси перед ним (nginx/gunicorn) отдал
транзитный `502`/`503`/`504` вместо JSON, необработанное исключение валит
весь процесс сырым traceback'ом. Отдельно от этого, `single_request()`
ретраит только сетевые обрывы/таймауты (`ConnectionError`, `ReadTimeout`), но
не ретраит уже полученный ответ с кодом 5xx — то есть даже кратковременный
сбой NetBox не давал ни единого шанса на повтор.

Это баг в самом netbox-sync (проверено сравнением с исходниками v1.8.1 —
там код идентичен), не связан с остальными фиксами форка и не является
следствием ни одного из внесённых изменений.

**Фикс:**
1. `single_request()` — при получении ответа с `status_code >= 500` теперь
   тоже ретраит (до `max_retry_attempts`, как и при сетевых обрывах), с
   паузой 2 секунды между попытками — на случай если это кратковременный
   сбой (например, перезапуск gunicorn worker'а на стороне NetBox).
2. `request()` — при постраничном запросе теперь явно проверяется
   `response.status_code` каждой страницы, а не только первой. Если после
   всех ретраев страница всё ещё возвращает не-200 — программа завершается
   с понятным сообщением об ошибке (`do_error_exit`, как и для первой
   страницы), а не сырым traceback'ом.

**Не устраняет первопричину** — если 502 повторяется стабильно (не
единичный случай), это означает проблему на стороне самого NetBox
(перегрузка, нехватка воркеров gunicorn, таймаут в nginx/reverse-proxy) —
стоит проверить логи NetBox/nginx на сервере NetBox в момент сбоя.

### 2.5. Конфликт устаревшего tagged_vlans при смене режима интерфейса на tagged-all

**Затронутые интерфейсы (из логов и первого боевого прогона):**
`vmnic2`/`vmnic3` на esxi04 (dcim/interfaces id 4581, 4582) — физические
аплинки в режиме `tagged-all`.

**Симптом:**
```
ERROR: NetBox returned: PATCH /api/dcim/interfaces/4581/ Bad Request
ERROR: NetBox returned body: {'tagged_vlans': ['Interface mode does not support tagged vlans']}
ERROR: Request Failed for interface. Used data: {'mtu': 9000, 'description': '25Gb/s  pNIC (vSanvDSwitch)', 'mode': 'tagged-all'}
```
Подтверждено по логам `netbox-sync-20260720-0630.log` — ошибка уже
присутствовала 20.07.2026, до любых правок форка. Не регрессия.

**Причина:** `module/sources/vmware/connection.py` формирует данные
интерфейса так, что ключ `tagged_vlans` добавляется в исходящие данные
**только** когда режим интерфейса — `tagged` (строки ~1897-1917, ~2027).
Для `tagged-all` (и `access`) ключ `tagged_vlans` просто не включается в
данные вообще — не как пустой список, а именно отсутствует. Если у этого
же интерфейса в NetBox с прошлого прогона (когда режим был `tagged` с
конкретными VLAN) уже записан непустой `tagged_vlans`, PATCH меняет только
`mode`, оставляя старое значение `tagged_vlans` как есть — а сочетание
`mode=tagged-all` + непустой `tagged_vlans` NetBox считает невалидным.

**Фикс (`module/netbox/connection.py`, по аналогии с self-heal кабелей из
п. 2.3):** при получении именно этой ошибки на PATCH интерфейса (`NBInterface`
или `NBVMInterface`) netbox-sync добавляет `tagged_vlans: []` к тем же данным
и повторяет PATCH один раз.

**Рекомендация:** это устраняет симптом автоматически при следующем
боевом прогоне — отдельных ручных действий в NetBox не требуется.

### 2.6. Self-heal кабелей (п. 2.3) не повторял PATCH после успешного удаления

**Файл:** `module/netbox/connection.py`, `NetBoxHandler.request()`

**Симптом** (боевой прогон против NetBox 4.6.4, 23.07.2026):
```
WARNING: interface id 4483 has an existing NetBox cable that conflicts with its synced type. Attempting to remove the stale cable and retry once.
WARNING: Self-heal for interface id 4483 cable conflict failed: '_CableRef' object must be a subclass of 'NetBoxObject'.
ERROR: NetBox returned: PATCH /api/dcim/interfaces/4483/ Bad Request
ERROR: NetBox returned body: {'type': ['Virtual interfaces cannot have a cable attached.']}
ERROR: Request Failed for interface. Used data: {...}
```
Повторилось на всех 7 интерфейсах из п. 2.3.

**Причина:** после успешного `DELETE` кабеля (NetBox отвечает `204 No Content`)
`request()` пытается залогировать понятное имя удалённого объекта через
`self.inventory.get_by_id(object_class, nb_id)`. Этот метод (в
`module/netbox/inventory.py`) рассчитан только на настоящие `NetBoxObject`
подклассы и явно проверяет это условие, вызывая исключение для всего
остального. `_CableRef` (см. п. 2.3) — намеренно **не** `NetBoxObject`
подкласс, поэтому вызов падает с `AttributeError`. Это исключение
перехватывалось собственным `try/except` в self-heal-функции и
отображалось как "self-heal failed" — притом что сам `DELETE` кабеля к
этому моменту уже успешно прошёл на стороне NetBox. Из-за этого
самолечение молча теряло последний шаг (повторный `PATCH` интерфейса),
и ошибка про кабель продолжала попадать в лог на каждом прогоне, пока
кабель уже не мешал (он был удалён), просто PATCH не повторялся в том же
запуске.

Баг присутствовал с момента написания фикса 2.3 (21.07.2026), но не был
замечен раньше: если ориентироваться только на NetBox Changelog UI
(вкладка "Журнал изменений" интерфейса), кабель к этому моменту уже
пропадал — DELETE ведь реально срабатывал, только сразу вслед за этим
падал повторный PATCH. Обнаружено только при построчном разборе полного
лога боевого прогона против NetBox 4.6.4.

**Фикс:** `request()` теперь ищет понятное имя объекта через
`inventory.get_by_id()` только если `object_class` — настоящий
`NetBoxObject` подкласс; для остальных случаев (как `_CableRef`) просто
использует численный ID в сообщении лога, без обращения к inventory.

**Практическое следствие:** конфликт кабеля на всех 7 интерфейсах из п. 2.3
должен теперь устраняться за один прогон, а не растягиваться на несколько
(кабель удаляется, и сразу следом успешно проходит PATCH интерфейса — без
повторной ошибки на следующем запуске).

---

## 3. Проверка совместимости с NetBox 4.3 → 4.6 (для апгрейда 4.2.6 → 4.6.4)

Проверено построчным grep по всему `module/` на предмет использования
конкретных breaking changes из release notes NetBox 4.3–4.6:

| Breaking change NetBox | Затрагивает netbox-sync? | Проверка |
|---|---|---|
| GraphQL: новый синтаксис фильтров (4.3), id/enum lookup (4.5) | Нет | `grep -ri graphql module/` — 0 совпадений, используется только REST (`requests`) |
| PostgreSQL 13 не поддерживается (4.3) | Нет | требование к серверу NetBox, не к netbox-sync |
| django-storages обязателен (4.3) | Нет | зависимость NetBox-сервера |
| `ALLOW_TOKEN_RETRIEVAL=False` по умолчанию (4.3) | Нет | netbox-sync только использует токен в заголовке `Authorization`, не обращается к `/api/users/tokens/` |
| `/api/extras/object-types/` → `/api/core/object-types/`, старый эндпоинт удалён (4.5) | Нет | netbox-sync не обращается к этому эндпоинту вообще; поле `object_types`/`content_types` на custom fields уже обрабатывается через version-gate `>= 4.0.0` (`object_classes.py:1308-1345`) |
| Python 3.12+ обязателен (4.5) | Да, но уже закрыто | решается переходом на Ubuntu 24.04 (по вашему плану) + `aiodns==4.0.0` (см. раздел 1) |
| Токены v2 (v1 всё ещё работает, deprecated) (4.5) | Нет действий не требуется | заголовок `Token <value>` формата не меняет, обратная совместимость заявлена NetBox |
| `/api/dcim/cable-terminations/` стал read-only (4.5) | Нет | это отдельный эндпоинт от `/api/dcim/cables/`, который остаётся полностью доступным для записи; наш фикс кабеля (2.3) использует именно `dcim/cables`, не `cable-terminations` |
| `render_config` требует отдельного права (4.5) | Нет | netbox-sync не рендерит конфиги устройств |
| PostgreSQL 14 / Redis 5.x deprecated, удаление в 4.7 (4.6) | Нет | требование к серверу NetBox, не к netbox-sync; учтено в общем плане апгрейда |
| Новая модель `MACAddress` вместо поля `mac_address` на интерфейсе (4.2) | Да, уже обработано | `Interface.mac_address` стал read-only property (зеркалит `primary_mac_address`) начиная с NetBox 4.2. netbox-sync уже вырезает `mac_address` из payload интерфейса и передаёт его через отдельный `NBMACAddress`-объект — см. `module/sources/common/source_base.py:270-273`, `324-361`, gate `>= version.parse("4.2.0")`. Единственный реальный баг в этой части — п. 2.1 выше, он исправлен |

**Вывод:** после переноса фиксов из раздела 1–2 у меня нет оснований полагать,
что апгрейд NetBox 4.2.6 → 4.6.4 сам по себе что-то сломает в netbox-sync
дополнительно к уже найденному. Собственно baseline-риск апгрейда (Python 3.12,
диск, PostgreSQL/Redis) уже описан в `00_ВЫВОДЫ_И_РЕКОМЕНДАЦИИ.md` из прошлого
чата и этим документом не отменяется.

**Важная оговорка:** это статический анализ кода, не прогон против реального
NetBox 4.6 инстанса. Обязательно протестировать на тестовом стенде перед продом
(шаги — в `DEPLOY_RU.md`).

---

## 4. Источники

- Issue о сворачивании поддержки: https://github.com/bb-Ricardo/netbox-sync/issues/474
- Релиз v1.8.1: https://github.com/bb-Ricardo/netbox-sync/releases/tag/v1.8.1
- Релиз v1.8.0: https://github.com/bb-Ricardo/netbox-sync/releases/tag/v1.8.0
- Issue #488 (aiodns/Python 3.12): https://github.com/bb-Ricardo/netbox-sync/issues/488
- Issue #446 (Missing API-Version): https://github.com/bb-Ricardo/netbox-sync/issues/446
- PR #490 (skip prune on source failure): https://github.com/bb-Ricardo/netbox-sync/pull/490
- NetBox issue #18768 (false-positive primary MAC unassign): https://github.com/netbox-community/netbox/issues/18768
- NetBox PR #18784 (фикс #18768): https://github.com/netbox-community/netbox/pull/18784
- Форк Sol1 (NetBox partner, предлагали maintainer-ство в #474): https://github.com/sol1/netbox-sync
